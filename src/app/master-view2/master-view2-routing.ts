import { Route } from '@vaadin/router';
import './resizing-issue/resizing-issue';

export const routes: Route[] = [
  { path: 'resizing-issue', component: 'app-resizing-issue', name: 'Resizing Issue' }
];
